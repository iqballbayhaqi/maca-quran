import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
    palette: {
      primary: {
        main: "#017901",
      },
      secondary: {
        main: "#ff9800",
      },
      grey: {
        main: "#b9b3cd"
      }
    },
  });

  export default theme;